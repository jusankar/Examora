services:
  examora:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: examora-app
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
